// Type definitions for frontend deployment (separate from backend)
// Using 'any' since we can't import actual backend types in production

export type AppRouter = any;

export type RouterInputs = any;
export type RouterOutputs = any;
